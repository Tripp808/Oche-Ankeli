/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: 'https://github.com/Tripp808/Oche-Ankeli',
}

module.exports = nextConfig

